default:
  extensions:
    Behat\MinkExtension\Extension:
      base_url: ${app.uri}
    Drupal\DrupalExtension\Extension:
      drush:
        root: ${app.root}
      drupal:
        drupal_root: ${app.root}

drupal7:
  extensions:
    Drupal\DrupalExtension\Extension:
      drupal:
        drupal_root: ${app.root}
      drush:
        root: ${app.root}
